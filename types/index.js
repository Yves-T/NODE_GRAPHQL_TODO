import {
    MutationType,
} from './mutationAdd';
import {
    queryType,
} from './queryType';
import {
    TodoType,
} from './todoType';

export let types = {
    MutationType,
    queryType,
    TodoType,
};
